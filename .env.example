# =============================================================================
# Your Claude Engineer - Environment Variables
# =============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# =============================================================================

# =============================================================================
# MCP Server URLs
# =============================================================================
# Deploy MCP servers from https://github.com/AxonCode/axon-mcp

# URL of your Task MCP Server
TASK_MCP_URL=https://mcp.yourdomain.com/task/sse

# URL of your Telegram MCP Server
TELEGRAM_MCP_URL=https://mcp.yourdomain.com/telegram/sse

# API key for MCP server authentication (Bearer token)
# The key is validated server-side via OAuth 2.0 or API key lookup.
# Create via: docker exec -it mcp-task python admin_cli.py create-key agent --name "Agent Key"
# The key is sent in Authorization header: "Bearer <key>"
MCP_API_KEY=mcp_your_api_key_here

# =============================================================================
# Agent Model Configuration (optional)
# =============================================================================

# Configure which model each agent uses. Options: haiku, sonnet, opus
# Sub-agents can also use "inherit" to use the orchestrator's model.

# Orchestrator - delegates to specialized agents (default: haiku)
# ORCHESTRATOR_MODEL=haiku

# Task Agent - project/issue management (default: haiku)
# TASK_AGENT_MODEL=haiku

# Coding Agent - writes code, tests, and manages git (default: sonnet)
# CODING_AGENT_MODEL=sonnet

# Telegram Agent - notifications (default: haiku)
# TELEGRAM_AGENT_MODEL=haiku

# =============================================================================
# Project Output Configuration
# =============================================================================

# Base directory where generated projects are created.
# Each project gets its own subfolder with a separate git repo.
# Default: ./generations (relative to the repo)
#
# Example: Set to an absolute path outside the repo to avoid git conflicts
# GENERATIONS_BASE_PATH=/home/user/projects/ai-generations

# =============================================================================
# Heartbeat Monitoring Configuration (optional)
# =============================================================================
# The heartbeat daemon runs in the axon-mcp Docker stack.
# These settings are used when running heartbeat locally for development.

# How often to run health checks (in minutes)
# HEARTBEAT_INTERVAL_MINUTES=5

# Hours without update to consider a task stale (used by Task_GetProjectStats and heartbeat)
# Default: 2 hours. Lower values are more aggressive, higher values are more lenient.
# STALE_THRESHOLD_HOURS=2

# Team key to monitor (optional, monitors all teams if not set)
# HEARTBEAT_TEAM=ENG

# =============================================================================
# Telegram Rich Reports Configuration (ENG-31)
# =============================================================================
# Scheduled reports: daily digest and weekly summary

# Hour to send daily digest (0-23, default: 18 = 6pm)
# DAILY_DIGEST_HOUR=18

# Day to send weekly summary (0=Monday, 6=Sunday, default: 4=Friday)
# WEEKLY_SUMMARY_DAY=4

# =============================================================================
# GitHub Integration Configuration
# =============================================================================
# Auto-push, PR creation, and GitHub Issues sync

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for push/PR), workflow (for status checks)
# The token is used for:
#   - Pushing branches to remote
#   - Creating pull requests
#   - Setting commit status checks
#   - Syncing with GitHub Issues (optional)
# GITHUB_TOKEN=ghp_your_token_here

# GitHub Repository (owner/repo format)
# If not set, will be detected from git remote origin
# GITHUB_REPO=AxonCode/your-claude-engineer

# Default base branch for PRs (default: main)
# GITHUB_BASE_BRANCH=main

# PR reviewers to request (comma-separated GitHub usernames)
# GITHUB_REVIEWERS=username1,username2

# Enable GitHub Issues sync (bidirectional)
# When enabled, Task MCP issues are synced to GitHub Issues
# Default: false (disabled)
# GITHUB_ISSUES_SYNC=false
