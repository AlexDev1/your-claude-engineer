# =============================================================================
# Task MCP Server Configuration
# =============================================================================
# URL of your Task MCP Server (PostgreSQL backend)
# Deploy on VDS and set the URL here
TASK_MCP_URL=http://your-vds:8001/sse

# =============================================================================
# Telegram MCP Server Configuration
# =============================================================================
# URL of your Telegram MCP Server
TELEGRAM_MCP_URL=http://your-vds:8002/sse

# =============================================================================
# Optional: API Key for MCP Server Authentication
# =============================================================================
# If you configure authentication on your MCP servers, set the key here
# MCP_API_KEY=your_secret_api_key

# =============================================================================
# Project Output Configuration
# =============================================================================
# Base directory where generated projects are created.
# Each project gets its own subfolder with a separate git repo.
# Default: ./generations (relative to the repo)
#
# Example: Set to an absolute path outside the repo to avoid git conflicts
# GENERATIONS_BASE_PATH=/Users/yourname/projects/ai-generations

# =============================================================================
# Agent Model Configuration (optional)
# =============================================================================
# Configure which model each agent uses. Options: haiku, sonnet, opus
# Sub-agents can also use "inherit" to use the orchestrator's model.
#
# Orchestrator - delegates to specialized agents (default: haiku)
# ORCHESTRATOR_MODEL=haiku
#
# Task Agent - project/issue management (default: haiku)
# TASK_AGENT_MODEL=haiku
#
# Coding Agent - writes code, tests, and manages git (default: sonnet)
# CODING_AGENT_MODEL=sonnet
#
# Telegram Agent - notifications (default: haiku)
# TELEGRAM_AGENT_MODEL=haiku

# =============================================================================
# MCP Server Deployment (for docker-compose on VDS)
# =============================================================================
# These variables are used when deploying the MCP servers

# PostgreSQL database password for Task MCP Server
# DB_PASSWORD=your_secure_database_password

# Telegram Bot Token (get from @BotFather)
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Telegram Chat ID for notifications (your personal chat or group)
# Get your chat ID by messaging @userinfobot
# TELEGRAM_CHAT_ID=123456789
