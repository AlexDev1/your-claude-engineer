# =============================================================================
# Your Claude Engineer - Переменные окружения
# =============================================================================
#
# Скопируйте этот файл в .env и заполните ваши значения:
#   cp .env.example .env
#
# =============================================================================

# =============================================================================
# URL MCP серверов
# =============================================================================
# Разверните MCP серверы из https://github.com/AxonCode/axon-mcp

# URL вашего Task MCP Server
TASK_MCP_URL=https://mcp.yourdomain.com/task/sse

# URL вашего Telegram MCP Server
TELEGRAM_MCP_URL=https://mcp.yourdomain.com/telegram/sse

# API ключ для аутентификации MCP сервера (Bearer токен)
# Ключ валидируется на стороне сервера через OAuth 2.0 или API key lookup.
# Создайте через: docker exec -it mcp-task python admin_cli.py create-key agent --name "Agent Key"
# Ключ передаётся в заголовке Authorization: "Bearer <key>"
MCP_API_KEY=mcp_your_api_key_here

# =============================================================================
# Конфигурация модели агента (опционально)
# =============================================================================

# Настройте какую модель использует каждый агент. Опции: haiku, sonnet, opus
# Субагенты также могут использовать "inherit" для использования модели оркестратора.

# Оркестратор - делегирует специализированным агентам (по умолчанию: haiku)
# ORCHESTRATOR_MODEL=haiku

# Task агент - управление проектами/задачами (по умолчанию: haiku)
# TASK_AGENT_MODEL=haiku

# Coding агент - пишет код, тесты и управляет git (по умолчанию: sonnet)
# CODING_AGENT_MODEL=sonnet

# Telegram агент - уведомления (по умолчанию: haiku)
# TELEGRAM_AGENT_MODEL=haiku

# =============================================================================
# Конфигурация вывода проекта
# =============================================================================

# Базовая директория где создаются генерируемые проекты.
# Каждый проект получает свою поддиректорию с отдельным git репозиторием.
# По умолчанию: ./generations (относительно репозитория)
#
# Пример: Установите абсолютный путь вне репозитория чтобы избежать git конфликтов
# GENERATIONS_BASE_PATH=/home/user/projects/ai-generations

# =============================================================================
# Конфигурация мониторинга Heartbeat (опционально)
# =============================================================================
# Heartbeat демон работает в Docker стеке axon-mcp.
# Эти настройки используются при локальной разработке heartbeat.

# Как часто выполнять проверки здоровья (в минутах)
# HEARTBEAT_INTERVAL_MINUTES=5

# Часов без обновления чтобы считать задачу застрявшей (используется Task_GetProjectStats и heartbeat)
# По умолчанию: 2 часа. Меньшие значения более агрессивны, большие значения более мягки.
# STALE_THRESHOLD_HOURS=2

# Ключ команды для мониторинга (опционально, мониторит все команды если не установлено)
# HEARTBEAT_TEAM=ENG

# =============================================================================
# Конфигурация отчётов Telegram Rich Reports (ENG-31)
# =============================================================================
# Запланированные отчёты: ежедневный дайджест и еженедельная сводка

# Час для отправки ежедневного дайджеста (0-23, по умолчанию: 18 = 6pm)
# DAILY_DIGEST_HOUR=18

# День для отправки еженедельной сводки (0=Понедельник, 6=Воскресенье, по умолчанию: 4=Пятница)
# WEEKLY_SUMMARY_DAY=4

# =============================================================================
# Конфигурация интеграции GitHub
# =============================================================================
# Auto-push, создание PR и синхронизация GitHub Issues

# GitHub Personal Access Token
# Создайте по адресу: https://github.com/settings/tokens
# Требуемые scopes: repo (для push/PR), workflow (для проверок статуса)
# Токен используется для:
#   - Отправки веток в remote
#   - Создания pull requests
#   - Установки проверок статуса коммита
#   - Синхронизации с GitHub Issues (опционально)
# GITHUB_TOKEN=ghp_your_token_here

# GitHub Repository (формат owner/repo)
# Если не установлено, будет определён из git remote origin
# GITHUB_REPO=AxonCode/your-claude-engineer

# Базовая ветка по умолчанию для PR (по умолчанию: main)
# GITHUB_BASE_BRANCH=main

# PR ревьюеры для запроса (имена пользователей GitHub через запятую)
# GITHUB_REVIEWERS=username1,username2

# Включить синхронизацию GitHub Issues (двунаправленную)
# Когда включено, задачи Task MCP синхронизируются с GitHub Issues
# По умолчанию: false (отключено)
# GITHUB_ISSUES_SYNC=false
